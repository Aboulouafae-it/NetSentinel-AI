/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // We don't use the SWC minifier locally to speed up builds sometimes, but leave default
};

module.exports = nextConfig;
