# Fortinet Real Redacted Syslog Captures

Place reviewed `real_redacted` FortiGate syslog samples here only after running
`tools/redact_device_capture.py` and manual review.

No raw firewall logs, usernames, public IPs, serial numbers, customer names, or
policy names should be committed.

No real captures are currently committed.
