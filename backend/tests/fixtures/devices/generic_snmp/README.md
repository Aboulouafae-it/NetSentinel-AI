# Generic SNMP Fixtures

Capture safe SNMP data with read-only walks for standard system and interface OIDs. Redact hostnames and interface descriptions that reveal customer topology.

The included samples are synthetic normalized poller output. They validate NetSentinel's parser behavior after SNMP transport normalization.
