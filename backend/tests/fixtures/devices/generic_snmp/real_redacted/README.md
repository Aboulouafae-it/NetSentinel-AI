# Generic SNMP Real Redacted Captures

Place reviewed `real_redacted` standard SNMP system/interface captures here
only after running `tools/redact_device_capture.py` and manual review.

No real captures are currently committed.
