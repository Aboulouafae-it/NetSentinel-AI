# TP-Link CPE Real Redacted Captures

Place reviewed `real_redacted` SNMP/manual RF captures here only after running
`tools/redact_device_capture.py` and manual review.

If CPE710 RF fields are absent, keep them absent and mark manual RF required in
metadata.

No real captures are currently committed.
