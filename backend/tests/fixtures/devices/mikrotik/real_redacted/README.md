# MikroTik Real Redacted Captures

Place reviewed `real_redacted` RouterOS captures here only after running
`tools/redact_device_capture.py` and manual review.

Required metadata file name per capture: `capture_metadata.json`.

No real captures are currently committed.
